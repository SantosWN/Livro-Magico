// Gerenciador de Música Infantil (Web Audio API) class GerenciadorDeMusica { constructor() { this.audioCtx = null; this.tocando = false; this.timer = null; this.notas = [261.63, 329.63, 392.00, 523.25, 493.88, 392.00, 329.63, 261.63]; this.indexNota = 0; } iniciar() { if (!this.audioCtx) { const AudioContext = window.AudioContext || window.webkitAudioContext; this.audioCtx = new AudioContext(); } if (this.audioCtx.state === 'suspended') { this.audioCtx.resume(); } if (!this.tocando) { this.tocando = true; this.tocarProximaNota(); } } parar() { this.tocando = false; if (this.timer) clearTimeout(this.timer); } tocarProximaNota() { if (!this.tocando || !this.audioCtx) return; const osc = this.audioCtx.createOscillator(); const gain = this.audioCtx.createGain(); osc.type = 'sine'; osc.frequency.setValueAtTime(this.notas[this.indexNota], this.audioCtx.currentTime); gain.gain.setValueAtTime(0.08, this.audioCtx.currentTime); gain.gain.exponentialRampToValueAtTime(0.0001, this.audioCtx.currentTime + 1.2); osc.connect(gain); gain.connect(this.audioCtx.destination); osc.start(); osc.stop(this.audioCtx.currentTime + 1.2); this.indexNota = (this.indexNota + 1) % this.notas.length; this.timer = setTimeout(() =&gt; this.tocarProximaNota(), 500); } } const gerenciadorMusica = new GerenciadorDeMusica(); function toggleMusica() { const btn = document.getElementById('music-toggle'); if (gerenciadorMusica.tocando) { gerenciadorMusica.parar(); btn.innerText = '🎵 Música: Off'; } else { gerenciadorMusica.iniciar(); btn.innerText = '🎶 Música: On'; } }